import java.util.*;public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();        while (t -- > 0) {            int n = sc.nextInt();            String x = sc.next();            if (reach(n, x)) {                System.out.println("YES");            }            else {                System.out.println("NO");            }        }    }    public static boolean reach(int n, String x) {        int mask = 1;        for (int i = 0; i < n; i++) {            char need = x.charAt(i);            int next = 0;            int t = (n - i) & 1;             if ((mask & 1) != 0) {                char left = 'a';                char right = (t == 1) ? 'a' : 'b';                if (need == '?' || need == left) next |= 2;                if (need == '?' || need == right) next |= 1;            }             if ((mask & 2) != 0) {                char left = 'b';                char right = (t == 1) ? 'b' : 'a';                if (need == '?' || need == left) next |= 1;                if (need == '?' || need == right) next |= 2;            }             mask = next;            if (mask == 0) return false;        }        return mask != 0;    }}