import java.util.*;public class Main {    public static void main(String[] args) {        Scanner fs = new Scanner(System.in);        int t = Integer.parseInt(fs.next());        StringBuilder out = new StringBuilder();        while (t-- > 0) {            int n = fs.nextInt();            char[] s = fs.next().toCharArray();             int ones = 0;            for (char c : s) if (c == '1') ones++;            int onesParity = ones & 1;             if ((n & 1) == 1 && onesParity == 1) {                out.append("-1\n");                continue;            }             int X;            if ((n & 1) == 0) X = onesParity;            else X = 0;             ArrayList<Integer> ops = new ArrayList<>();            for (int i = 0; i < n; i++) {                int a = s[i] - '0';                int xi = a ^ X;                if (xi == 1) ops.add(i + 1);            }             out.append(ops.size()).append('\n');            if (ops.size() == 0) {                out.append('\n');            } else {                for (int i = 0; i < ops.size(); i++) {                    if (i > 0) out.append(' ');                    out.append(ops.get(i));                }                out.append('\n');            }        }        System.out.print(out.toString());    }}