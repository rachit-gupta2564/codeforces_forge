import java.io.*;import java.util.StringTokenizer; public class Main {    static int[] parent = new int[800005];    static long[] weight = new long[800005];     static int find(int i) {        if (parent[i] == i) return i;        return parent[i] = find(parent[i]);    }     static void unite(int i, int j) {        int root_i = find(i);        int root_j = find(j);        if (root_i != root_j) {            parent[root_i] = root_j;            weight[root_j] += weight[root_i];        }    }     static void solve(FastScanner sc, PrintWriter out) {        int n = sc.nextInt();        int m = sc.nextInt();                int[] w = new int[(n - 1) * m];        for (int i = 0; i < (n - 1) * m; i++) {            w[i] = sc.nextInt();        }                int[] v = new int[n * (m - 1)];        for (int i = 0; i < n * (m - 1); i++) {            v[i] = sc.nextInt();        }                char[][] p = new char[n - 1][];        for (int i = 0; i < n - 1; i++) {            p[i] = sc.next().toCharArray();        }                char[][] q = new char[n][];        for (int i = 0; i < n; i++) {            q[i] = sc.next().toCharArray();        }                int total_nodes = (n + 1) * (m + 1);        for (int i = 0; i < total_nodes; i++) {            parent[i] = i;            weight[i] = 0;        }                for (int i = 1; i < n; i++) {            for (int j = 1; j < m; j++) {                int w_val_1 = w[(i - 1) * m + (j - 1)];                int w_val_2 = w[(i - 1) * m + j];                int v_val_1 = v[(i - 1) * (m - 1) + (j - 1)];                int v_val_2 = v[i * (m - 1) + (j - 1)];                                weight[i * (m + 1) + j] = 1L * w_val_1 - w_val_2 + v_val_1 - v_val_2;            }        }                for (int i = 0; i <= n; i++) {            unite(0, i * (m + 1));            unite(0, i * (m + 1) + m);        }        for (int j = 0; j <= m; j++) {            unite(0, j);            unite(0, n * (m + 1) + j);        }                for (int i = 0; i < n - 1; i++) {            for (int j = 0; j < m; j++) {                if (p[i][j] == '0') {                    unite((i + 1) * (m + 1) + j, (i + 1) * (m + 1) + j + 1);                }            }        }                for (int i = 0; i < n; i++) {            for (int j = 0; j < m - 1; j++) {                if (q[i][j] == '0') {                    unite(i * (m + 1) + j + 1, (i + 1) * (m + 1) + j + 1);                }            }        }                long ans = 0;        int boundary_root = find(0);                for (int i = 0; i < total_nodes; i++) {            if (parent[i] == i && i != boundary_root) {                if (weight[i] > 0) {                    ans += weight[i];                }            }        }                out.println(ans);    }     public static void main(String[] args) {        FastScanner sc = new FastScanner();        PrintWriter out = new PrintWriter(System.out);                int t = sc.nextInt();        while (t-- > 0) {            solve(sc, out);        }                out.flush();        out.close();    }     // FastScanner class for competitive programming I/O performance    static class FastScanner {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next() {            while (!st.hasMoreTokens()) {                try {                    String line = br.readLine();                    if (line == null) return null;                    st = new StringTokenizer(line);                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }    }}